# Design System

## Direction

视觉方向为“温暖黄昏里的双人胶片”。界面像一次约会结束后正在显影的照片：熟杏色夕光、莓红重点、暮色紫阴影和奶油底色共同营造明显但高级的情侣氛围。照片、两个人的身份和回应痕迹是主角；胶片编号、日期戳与轻微漏光只用于建立记忆点，不制造复古道具堆砌。

## Color

- 页面背景：`#F7EDE2`
- 主表面：`#FFF9F2`
- 主文字：`#2D2425`
- 次文字：`#7D6C69`
- 分隔线：`#E3CEC2`
- 心动主色：`#B94E5A`
- 黄昏杏色：`#E49A6A`
- 暮色紫：`#665060`
- 共同回应：`#6F7B62`
- 错误底色：`#F8E3DF`

颜色不单独承担状态表达，必须同时配合文字、图标或结构变化。

## Typography

- 界面统一使用系统中文无衬线字体：PingFang SC、Microsoft YaHei、system-ui。
- 页面标题 44 至 56rpx，700 字重，1.2 至 1.3 行高。
- 正文 25 至 28rpx，1.55 至 1.7 行高。
- 按钮文字 22 至 28rpx，600 字重，禁止使用书法字体。
- 紧凑按钮必须设置明确宽度、`min-width: 0` 和 `white-space: nowrap`。

## Components

- 主按钮使用 22rpx 圆角，不使用过度胶囊形状。
- 顶部工具按钮必须同时提供图标和文字，不能只用含义模糊的单字。
- 底部导航内容区固定为 112rpx，安全区单独追加。使用五个真实 Flex 槽位：甜蜜、我们、记录、回忆、我的，每个槽位拥有独立完整热区。
- 中间记录按钮固定 92rpx，并限制在第三槽的 112rpx 点击盒中，不悬浮遮挡相邻导航项。
- 列表行使用全宽结构，不使用原生按钮默认尺寸承载复杂内容。
- 时刻卡采用胶片框架：顶部日期戳、真实媒体、底部正文与回应，不做通用图标卡网格。
- 照片优先保持 4:5 或原始比例，黄昏色只存在于界面背景与装饰，不给所有用户照片强加滤镜。
- 情侣身份使用双头像、两种低饱和身份色和共同回应痕迹表达，不用爱心数量表达亲密。
- 卡片圆角控制在 18 至 24rpx，阴影只用于媒体或可交互内容层。

## Motion

- 状态切换和内容出现使用 180 至 240ms。
- 缓动使用 `cubic-bezier(.22,1,.36,1)`，禁止弹跳和大幅位移。
- 点击反馈使用轻微缩放和透明度变化。
- 动效只用于按钮反馈、筛选展开、列表出现和选中状态，不做装饰性循环动画。
- 系统要求减少动态效果时关闭非必要动画。

## Layout

- 页面左右安全边距为 32rpx。
- 信息块之间使用 20、28、38rpx 的节奏，不采用所有区域相同间距。
- 页面底部内容留白必须覆盖底部导航高度和设备安全区。
- 等分选择器和重复列表优先使用 Flex 百分比分栏，子项使用 `flex: 1 1 0`、`width: 0` 和 `min-width: 0`，避免安卓 WebView 的 Grid 最小内容宽度导致横向溢出。
